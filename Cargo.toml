[package]
name = "memea"
authors = ["Zephan Enciso <zmenciso@pm.me>"]
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.21", features = ["derive"] }
derive_more = { version = "1.0.0", features = ["add", "mul"] }
